import {useEffect,useRef} from 'react';
type Tool={name:string;description:string;inputSchema:object;annotations:{readOnlyHint:boolean};execute:(input:unknown)=>unknown};
type ModelDocument=Document&{modelContext?:{registerTool:(tool:Tool,options:{signal:AbortSignal})=>void|Promise<void>}};
export function useGameTools(state:object,join:(name:string,index:number)=>Promise<void>,tap:()=>Promise<void>,start:()=>Promise<void>){const current=useRef({state,join,tap,start});current.current={state,join,tap,start};useEffect(()=>{const context=(document as ModelDocument).modelContext;if(!context)return;const lifecycle=new AbortController();const tools:Tool[]=[{name:'start_race',description:'Start the room countdown, or play again from the results. Requires joining first.',inputSchema:{type:'object',properties:{},additionalProperties:false},annotations:{readOnlyHint:false},execute:async()=>{await current.current.start();return {started:true}}},{name:'read_race',description:'Read the current shared river phase and server standings.',inputSchema:{type:'object',properties:{},additionalProperties:false},annotations:{readOnlyHint:true},execute:()=>current.current.state},{name:'join_race',description:'Choose a duck and join the shared river, or spectate until the next race.',inputSchema:{type:'object',properties:{name:{type:'string',maxLength:14},duckIndex:{type:'integer',minimum:0,maximum:7}},required:['name','duckIndex'],additionalProperties:false},annotations:{readOnlyHint:false},execute:async(input)=>{const p=input as {name:string;duckIndex:number};if(!p||typeof p.name!=='string'||p.name.length>14||!Number.isInteger(p.duckIndex)||p.duckIndex<0||p.duckIndex>7)throw Error('Use a name of at most 14 characters and a duck index from 0 to 7.');await current.current.join(p.name,p.duckIndex);return {joined:true}}},{name:'tap_duck',description:'Send one tap to the authoritative river server. Only active racers can move.',inputSchema:{type:'object',properties:{},additionalProperties:false},annotations:{readOnlyHint:false},execute:async()=>{await current.current.tap();return {tapSent:true}}}];for(const tool of tools)try{void Promise.resolve(context.registerTool(tool,{signal:lifecycle.signal})).catch(()=>{})}catch{}return()=>lifecycle.abort()},[])}
